# Playwright Browsers

This framework uses Playwright.

After `npm i`, you may need to download browser binaries:

```bash
cd agents/browser-agent-framework
npx playwright install
```

Without this, Playwright will fail at runtime with "Executable doesn't exist".
